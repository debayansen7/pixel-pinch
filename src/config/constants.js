// Standard thumbnail size presets (width x height in pixels)
const THUMBNAIL_SIZES = {
    small:  { width: 150, height: 150 },  // Avatars, icon grids
    medium: { width: 300, height: 300 },  // Card thumbnails, gallery previews (default)
    large:  { width: 600, height: 600 },  // Hero cards, featured images
};

module.exports = { THUMBNAIL_SIZES };
